<form method="post" action="{{ path('app_categorie_v_delete', {'id': categorie_v.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_v.id) }}">
    <button class="btn">Supprimer</button>
</form>
