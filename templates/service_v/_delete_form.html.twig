<form method="post" action="{{ path('app_service_v_delete', {'id': service_v.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_v.id) }}">
    <button class="btn">Supprimer</button>
</form>
