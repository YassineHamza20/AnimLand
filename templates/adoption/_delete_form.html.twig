<form method="post" action="{{ path('app_adoption_delete', {'id': adoption.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adoption.id) }}">
    <button class="btn">Delete</button>
</form>
